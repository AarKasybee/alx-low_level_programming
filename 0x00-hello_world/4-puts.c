#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *string_word="\"Programming is like building a multilingual puzzle";
	puts(string_word);
	return (0);
}
